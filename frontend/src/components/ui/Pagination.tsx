type PaginationProps = { page: number; totalPages: number; onChange: (page: number) => void }
export function Pagination({ page, totalPages, onChange }: PaginationProps) { return <nav aria-label="Paginação" className="flex items-center gap-1"><button type="button" disabled={page <= 1} onClick={() => onChange(page - 1)} className="min-h-10 rounded-lg border border-contorno px-3 text-sm disabled:opacity-40">Anterior</button><span className="px-3 text-sm text-texto-secundario">{page} de {totalPages}</span><button type="button" disabled={page >= totalPages} onClick={() => onChange(page + 1)} className="min-h-10 rounded-lg border border-contorno px-3 text-sm disabled:opacity-40">Próxima</button></nav> }
